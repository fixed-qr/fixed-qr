import { AppText, AppView } from "@/components/app-ui";
import { StyleSheet } from "react-native";

export default function Maintenance() {
  return (
    <AppView>
      <AppText>Maintenance</AppText>
    </AppView>
  );
}

const styles = StyleSheet.create({});
