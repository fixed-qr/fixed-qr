import { AppText, AppView } from "@/components/app-ui";
import { StyleSheet } from "react-native";

export default function Discontinued() {
  return (
    <AppView>
      <AppText>Discontinued</AppText>
    </AppView>
  );
}

const styles = StyleSheet.create({});
